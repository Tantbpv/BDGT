import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Labels — BDGT' };

export default function LabelsPage() {
  return (
    <div>
      <h1>Labels</h1>
    </div>
  );
}
